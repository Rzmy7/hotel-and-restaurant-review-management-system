# Embedding Service Configuration
VITE_EMBEDDING_SERVICE_URL=http://localhost:8001

# Note: Gemini API key is managed through the admin panel (Settings > API Settings)
# but can also be set in the embedding service backend/.env if needed
