# ═══════════════════════════════════════════════════════════════════
# Server 2 — Backend .env Template
# Place this file at /opt/reviewmate/.env on the backend server
# ═══════════════════════════════════════════════════════════════════

# ── General ─────────────────────────────────────────────────────────
SECRET_KEY=<generate-a-strong-random-key>
ENVIRONMENT=production
FRONTEND_URL=https://reviewmate.live
ADMIN_FRONTEND_URL=https://admin.reviewmate.live

# ── CORS (optional extra origins, comma-separated) ──────────────────
# CORS auto-includes FRONTEND_URL and ADMIN_FRONTEND_URL. Add extras here:
# CORS_ORIGINS=https://other-domain.com

# ── SQLAlchemy ──────────────────────────────────────────────────────
DATABASE_URL=mssql+pyodbc://<user>:<password>@<server>/ScraperEngine?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes&Encrypt=no

# ── PYODBC ──────────────────────────────────────────────────────────
DB_DRIVER=ODBC Driver 18 for SQL Server
DB_SERVER=<your-db-server>
DB_NAME=ScraperEngine
DB_UID=<your-db-user>
DB_PWD=<your-db-password>

# ── SMTP ────────────────────────────────────────────────────────────
SMTP_EMAIL=<your-smtp-email>
SMTP_PASSWORD=<your-app-password>
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# ── Google OAuth ────────────────────────────────────────────────────
GOOGLE_CLIENT_ID=<your-google-client-id>
GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# ── JWT ─────────────────────────────────────────────────────────────
JWT_SECRET_KEY=<generate-a-long-random-string>

# ── Google Generative AI (Gemini) ───────────────────────────────────
GENAI_KEY=<your-gemini-api-key>

# ── Supabase ────────────────────────────────────────────────────────
SUPABASE_URL=<your-supabase-url>
SUPABASE_KEY=<your-supabase-anon-key>
SUPABASE_BUCKET=profile-images

# ── Microservice URLs (point to the other servers) ──────────────────
SCRAPER_ENGINE_URL=https://scrape.reviewmate.live
EMBEDDING_SERVICE_URL=https://embed.reviewmate.live

# ── WebSocket relay (backend → scraper engine for live sync progress) ─
# This is the WS base URL used by the backend to connect to the scraper
# engine's WebSocket endpoint. If unset, it is derived from
# SCRAPER_ENGINE_URL by replacing http(s) with ws(s) and appending /ws/jobs.
# SCRAPER_WS_URL=wss://scrape.reviewmate.live/ws/jobs
