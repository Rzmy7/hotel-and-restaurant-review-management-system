import React from "react";
import SupportTemplate from "../components/support/templates/SupportTemplate";

/**
 * SupportPage Component.
 *
 * Entry point for users seeking direct assistance.
 * Includes ticket submission, FAQ, and enterprise support lines.
 *
 * @returns {React.FC} The support gateway page.
 */
const SupportPage: React.FC = () => {
  return <SupportTemplate />;
};

export default SupportPage;
